<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Analitica Inmobiliario</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
  <nav role="navigation" class="justify-end-ns">
        <%= if Pow.Plug.current_user(@conn) do %>
        <h5>
          <div class="link"><%= gettext "Welcome %{user}", user: @current_user.email%></div>
        </h5>
          <div class="hover-blue link">
            <%= link "Sign out", to: Routes.pow_session_path(@conn, :delete), method: :delete %>
            </div>
        <p></p>
        <% else %>
        <h5>
        <div class="f16 fw16 hover-blue link mr4">
          <span><%= link "Register", to: Routes.pow_registration_path(@conn, :new) %></span>
        </div>
        <% end %>
    
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
